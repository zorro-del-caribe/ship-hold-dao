{
  "name": "ship-hold-dao",
  "version": "0.0.2",
  "description": "data access object or model instance extension for ship-hold",
  "main": "./src/index.js",
  "scripts": {
    "test": "node ./test/setup.js && node ./test/index.js | tap-spec",
    "build": "babel src --out-dir dist -s"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "keywords": [
    "dao",
    "database",
    "ship-hold",
    "orm",
    "model"
  ],
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/zorro-del-caribe/ship-hold-dao.git"
  },
  "author": "Laurent Renard",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.10.1",
    "babel-preset-es2015": "^6.9.0",
    "ship-hold": "^1.0.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.0"
  }
}
